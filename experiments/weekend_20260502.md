# Weekend 2026-05-02 Multi-Species + Call-Type Experiments

Goal: improve ONC deployment performance for multi-species and call-type prediction by adding bounded external-data experiments, starting with BioDCASE Task 2 and later DCLDE killer-whale data.

## Current Baselines

- ONC balanced100 species-only run `13043617`: threshold-0.5 macro F1 `0.5764`; per-label calibrated macro F1 `0.6132`.
- ONC balanced200 run `13057931`: threshold-0.5 macro F1 `0.5691`; per-label calibrated macro F1 `0.5860`.
- Main weakness from ONC-only error review: OD/Oo/background confusion, especially false positives on background and cross-confusion between OD and Oo.

## Completed Data Prep

- BioDCASE train50 prep job `13110064` completed successfully.
- Run directory: `/project/6070467/merileo/data/finwhales/final2025_resnet_20260423/multispecies_calltype_experiments/prep_runs/biodcase_task2_prep_train50_resume_20260501T182239Z`.
- Outputs: 450 MATs, 450 staged audio items, zero missing audio, zero split leakage.
- Label counts: 100 background, 200 `species:Bm`, 150 `species:Bp`, and 50 each of `BmA`, `BmB`, `BmD`, `BmZ`, `Bp20`, `Bp20plus`, `BpD`.

## Phase 1 Jobs

Use Nibi output root:

`$SCRATCH/whale-call-analysis/multispecies_weekend_20260502/`

Launcher:

```bash
bash drac/scripts/submit_multispecies_weekend_phase1.sh
```

Planned bounded GPU jobs:

- `E01_onc_bal100_control`: ONC balanced100, canonical species only.
- `E02_biodcase_train50_only`: BioDCASE train50, canonical species plus call types.
- `E03_onc_biod_train50_species`: ONC balanced100 + BioDCASE train50, canonical species only.
- `E04_onc_biod_train50_species_call`: ONC balanced100 + BioDCASE train50, canonical species plus call types.

Submitted on Nibi after queue/accounting checks:

- `13137809` `E01_onc_bal100_control`
- `13137811` `E02_biodcase_train50_only`
- `13137813` `E03_onc_biod_train50_species`
- `13137814` `E04_onc_biod_train50_species_call`

Run directories are listed in:

`/scratch/merileo/whale-call-analysis/multispecies_weekend_20260502/weekend_phase1_submitted.tsv`

W&B project: `whale-multispecies-calltype`.

Gate: scale BioDCASE only if an ONC source-stratified validation metric beats calibrated primary-species macro F1 `0.6132`, or materially improves Bm/Bp/call performance without dropping Mn/Oo/background behavior by more than `0.03`.

## Monitoring Notes

- Before every Slurm submission, run `squeue -u merileo` and `sacct -u merileo --starttime now-7days`.
- CPU is acceptable for prep and manifest work; training must use GPU.
- Monitor job logs, `run_summary.json`, `source_metrics_summary.json`, `per_class_metrics.csv`, `threshold_sweep_summary.json`, W&B run status, and disk/file counts.
- Notify on job completion, job failure, quota/disk risk, or gate decision.

## Phase 1 Results

Checked 2026-05-02 after jobs completed:

- `E01_onc_bal100_control` completed in `00:01:26`; ONC-only per-label calibrated primary-species macro F1 `0.6372`; background any-primary FP rate `0.4138`.
- `E02_biodcase_train50_only` completed in `00:00:52`; BioDCASE per-label calibrated macro F1 `0.6694`.
- `E03_onc_biod_train50_species` completed in `00:00:53`; ONC-only per-label calibrated primary-species macro F1 `0.6388`; background any-primary FP rate `0.6552`.
- `E04_onc_biod_train50_species_call` completed in `00:00:55`; ONC-only per-label calibrated primary-species macro F1 `0.6549`; background any-primary FP rate `0.6897`.

Interpretation: BioDCASE plus call labels improved ONC primary-species calibrated macro F1, but background false positives worsened substantially. This is not a clean scale-up signal for ONC deployment. Next step should be a constrained-threshold/background-error analysis and FP image review before launching cap500/cap1000 training.

## Phase 2 No-Pos-Weight Recovery

Submitted after queue/accounting checks under:

`/scratch/merileo/whale-call-analysis/multispecies_weekend_20260502/phase2_nopos_gpu40/`

- `13149664` `E03_onc_biod_train50_species`, no `pos_weight`, 40 epochs, GPU.
- `13149666` `E04_onc_biod_train50_species_call`, no `pos_weight`, 40 epochs, GPU.

Checked 2026-05-02 after both completed:

- `E03_nopos_species`: ONC-only per-label calibrated primary-species macro F1 `0.6036`; micro F1 `0.5882`; background any-primary FP rate `0.6897` (`20/29`).
- `E04_nopos_species_call`: ONC-only per-label calibrated primary-species macro F1 `0.5802`; micro F1 `0.5517`; background any-primary FP rate `0.6897` (`20/29`).

Decision: no-pos-weight did not recover background rejection and hurt ONC macro F1. Do not scale this branch.

## Phase 3 Background-Recovery Probe

Because Phase 1 improved ONC calibrated primary-species macro F1 but caused too many background false positives, and Phase 2 did not fix it, the next bounded probe uses the richer ONC balanced200 prep plus BioDCASE train50 with `pos_weight` restored.

Submitted under:

`/scratch/merileo/whale-call-analysis/multispecies_weekend_20260502/phase3_onc_bal200_biod_train50_pos_gpu40/`

- `13150115` `E03_onc_biod_train50_species`, ONC balanced200 + BioDCASE train50, species only, 40 epochs, GPU.
- `13150116` `E04_onc_biod_train50_species_call`, ONC balanced200 + BioDCASE train50, species + call labels, 40 epochs, GPU.

Gate: advance only if ONC-only calibrated primary-species macro F1 is at least comparable to `E01` (`0.6372`) while substantially reducing the background any-primary FP rate versus the Phase 1 combined runs (`0.6552` to `0.6897`). If macro F1 or background rejection is worse, stop scaling and inspect FP/FN examples or move to a DCLDE Oo audit.

Checked 2026-05-02 after both jobs completed:

- `13150115` `E03_bal200_pos_species`: ONC-only per-label calibrated primary-species macro F1 `0.5981`; micro F1 `0.5957`; background any-primary FP rate `0.6102` (`36/59`).
- `13150116` `E04_bal200_pos_species_call`: ONC-only per-label calibrated primary-species macro F1 `0.5691`; micro F1 `0.5502`; background any-primary FP rate `0.8305` (`49/59`).

Decision: ONC balanced200 plus BioDCASE train50 did not pass the background-recovery gate. It reduced FP a little for species-only compared with the worst combined runs, but ONC primary macro F1 fell below both `E01` and the original `13043617` calibrated baseline. The species+call variant was worse on both macro F1 and background rejection. Do not launch cap500/cap1000 BioDCASE training from this branch.

## Automation State

Created heartbeat automation `monitor-nibi-multispecies-weekend` to continue the weekend loop from this thread. The next bounded action should be non-training: summarize FP/FN examples and audit DCLDE 2026 killer-whale metadata/audio availability under scratch before any Oo-repair prep or GPU training.

## Heartbeat 2026-05-02 12:30 PDT

Nibi status:

- No active weekend multispecies Slurm jobs.
- `squeue -u merileo` showed one unrelated active `fin_yolo26` job (`13143366`) on `g32`; no action taken.
- Weekend scratch root was about `381M` and `717` files before the DCLDE annotation audit download.

FP/FN artifacts are available for review:

- `E01_onc_bal100_control_20260502T074301Z`: 24 example PNGs, split evenly between 12 false positives and 12 false negatives; 87 validation rows.
- `E04_onc_biod_train50_species_call_20260502T074323Z`: 24 PNGs, 12 FP / 12 FN; 154 validation rows.
- `phase2` no-pos-weight `E03` and `E04`: 24 PNGs each, 12 FP / 12 FN; 154 validation rows each.
- `phase3` ONC balanced200 + BioDCASE `E03` and `E04`: 24 PNGs each, 12 FP / 12 FN; 219 validation rows each.

DCLDE killer-whale audit:

- The DOI landing page now names this dataset as **DCLDE 2027** because the workshop was postponed, though the earlier plan and paper refer to DCLDE 2026.
- Public landing/catalog sources:
  - NOAA/Data.gov: https://catalog.data.gov/dataset/dclde-2027-killer-whale-orcinus-orca-ecotype-and-other-species-annotations-for-the-detecti-2027
  - Nature Scientific Data article: https://www.nature.com/articles/s41597-025-05281-5
  - DOI landing page: https://doi.org/10.25921/15EY-MH50
  - GCS root: `gs://noaa-passive-bioacoustic/dclde/2027/dclde_2027_killer_whales/`
- Staged only metadata/annotations under scratch, not the full audio corpus:
  - `/scratch/merileo/whale-call-analysis/multispecies_weekend_20260502/audits/dclde_2027_killer_whales/Annotations.csv`
  - `/scratch/merileo/whale-call-analysis/multispecies_weekend_20260502/audits/dclde_2027_killer_whales/CC-BY-4.0.txt`
  - `/scratch/merileo/whale-call-analysis/multispecies_weekend_20260502/audits/dclde_2027_killer_whales/audit_summary.json`
- GCS audit found `13,419` objects totaling about `1.62 TB`; `13,091` are audio objects. This is not something to mirror wholesale.
- Top-level provider folders: `dfo_crp`, `dfo_wdlp`, `onc`, `orcasound`, `scripps`, `simres`, `smru`, `uaf`, `vfpa`.
- Collated `Annotations.csv` is `50,484,452` bytes with `207,574` rows and `13,084` unique sound files.
- Columns: `Soundfile`, `Dataset`, `LowFreqHz`, `HighFreqHz`, `FileEndSec`, `UTC`, `FileBeginSec`, `ClassSpecies`, `KW`, `KW_certain`, `Ecotype`, `Provider`, `AnnotationLevel`, `FilePath`, `FileOk`.
- Class/species counts: `HW` 124,977; `KW` 59,107; `UndBio` 11,821; `AB` 11,669.
- Ecotype/community counts: `NA` 154,917; `SRKW` 20,908; `TKW` 12,707; `NRKW` 8,266; `SAR` 8,078; `OKW` 2,698.
- Provider counts: `DFO_CRP` 158,874; `JASCO_VFPA` 12,517; `DFO_WDLP` 10,408; `UAF_NGOS` 10,113; `SIMRES` 4,018; `SIO` 2,795; `ONC` 2,706; `OrcaSound` 2,398; `JASCO_VFPA_ONC` 2,078; `SMRUConsulting` 1,667.

Prep proposal:

- Do not download full DCLDE audio.
- Implement a DCLDE manifest builder that reads the collated `Annotations.csv`, maps `ClassSpecies=KW` to `species:Oo`, maps `HW` to `species:Mn` or analysis confounder depending on the experiment, and keeps `UndBio`/`AB` as hard-negative or analysis metadata.
- Use a bounded `dclde_orca_cap200` CPU prep first:
  - max `Oo` positives: 200, source-balanced across at least ONC/OrcaSound/DFO/JASCO if audio availability allows.
  - background/confounders: cap 200 from `HW`, `UndBio`, and `AB`, preferably frequency/time-overlapping with KW examples.
  - group splits by provider + dataset + soundfile to avoid leakage.
  - preserve `Ecotype`, `Provider`, `Dataset`, `LowFreqHz`, `HighFreqHz`, and original row id as metadata.
- Only after that prep passes coverage/leakage checks should we submit a bounded GPU Oo-repair run.

## Heartbeat 2026-05-02 13:25 PDT

DCLDE prep path implementation:

- Added `scripts/data/multilabel/build_dclde_killer_whale_manifest.py`.
- Added focused tests in `tests/test_dclde_killer_whale_manifest.py`.
- Added CPU-only Nibi submitter `drac/scripts/submit_dclde_orca_prep_smoke.sh`.
- Local checks passed: DCLDE manifest tests, BioDCASE manifest tests, multilabel tests, multispecies prep manifest tests, `py_compile`, and submitter `bash -n`.
- Commit pushed: `947af4f` (`Add DCLDE killer whale prep path`).

Nibi checks and submission:

- Pulled `947af4f` on Nibi.
- Nibi checks passed: focused DCLDE unittest discovery, DCLDE builder `py_compile`, and submitter `bash -n`.
- Queue/accounting checks were run before submission.
- Submitted bounded CPU prep job `13154167`.
- Run dir: `/scratch/merileo/whale-call-analysis/multispecies_weekend_20260502/prep_runs/dclde_orca_cap200_prep_20260502T202327Z`.
- At submission it was pending with 4 CPU, 40G memory, 12 hour walltime.

Next gate:

- Monitor job `13154167` only as prep/download/MAT generation.
- Inspect `run_metadata.json`, `prep_manifest/prep_summary.json`, `raw_audio/staging_summary.json`, `mat_files/report.csv`, and `splits/split_summary.json`.
- Submit no GPU training until the prep has representative KW/confounder coverage, zero failed staged audio, generated MATs for selected rows, and leak-free grouped splits.

## Heartbeat 2026-05-02 14:50 PDT

DCLDE prep failure and retry:

- CPU prep job `13154167` failed during MAT generation after selected DCLDE audio had staged successfully.
- Staging summary before the failure: `400` selected rows, `184` unique staged audio files, `0` failed downloads.
- Failure cause: one DCLDE source audio file was multi-channel, and `prepare_trainstyle_windows.py` concatenated 2-D audio with 1-D padding/context arrays.
- Added commit `d89cc8f` (`Handle multi-channel audio in trainstyle prep`) to downmix multi-channel audio to mono before context padding in both train-style prep entrypoints.
- Local checks passed for the patch: `py_compile` on both prep scripts plus DCLDE/BioDCASE/multilabel/multispecies unit tests.
- Pulled the patch on Nibi, ran `py_compile`, and reran queue/accounting checks before retry submission.
- Submitted CPU retry job `13156974` using the existing run directory and already staged audio, without re-downloading the DCLDE subset.
- Retry job script: `/scratch/merileo/whale-call-analysis/multispecies_weekend_20260502/prep_runs/dclde_orca_cap200_prep_20260502T202327Z/logs/dclde_orca_cap200_retry_mono_20260502T214949Z.sbatch`.

Active DCLDE prep run remains:

`/scratch/merileo/whale-call-analysis/multispecies_weekend_20260502/prep_runs/dclde_orca_cap200_prep_20260502T202327Z`

Next heartbeat should monitor retry job `13156974`, then inspect the same prep summaries and leakage gates before considering any GPU training.

## Heartbeat 2026-05-02 15:40 PDT

DCLDE prep retry status:

- Retry job `13156974` failed during MAT generation on a staged DCLDE UAF WAV file: `dclde_UAF_NGOS_Field_HTI__20190520_1951.wav`.
- The public GCS object returned a valid RIFF/WAV header, so the issue was a corrupt or partial staged local copy rather than a bad source object.
- Removed and redownloaded only that one 5.4 GB source audio file from GCS without resume/append.
- Submitted another bounded CPU retry using the existing mono-patched retry script and the existing run directory.
- New active retry job: `13159234`.
- Run dir remains `/scratch/merileo/whale-call-analysis/multispecies_weekend_20260502/prep_runs/dclde_orca_cap200_prep_20260502T202327Z`.

Next heartbeat should monitor job `13159234`, especially whether MAT generation progresses past the prior UAF file and reaches split generation.

## Heartbeat 2026-05-02 16:40 PDT

DCLDE prep retry status:

- Retry job `13159234` failed again on the same UAF file, even after the local copy was redownloaded and had a RIFF/WAV header.
- `file` identifies the object as 24-bit stereo 48 kHz WAV, but `libsndfile` still refuses it, likely because this large classic-WAV object is not readable by the current `soundfile` stack.
- To keep the prep bounded and moving, filtered every row for `dclde_UAF_NGOS_Field_HTI__20190520_1951.wav` out of `selected_calls.csv`, `expected_multilabel_manifest.csv`, `positive_calls.csv`, and `required_audio_sources.csv`.
- This removed three KW rows from the cap200 subset: filtered manifests now have `397` selected rows and `197` positive rows.
- Queue/accounting checks were run before resubmission.
- New active retry job: `13161097`.
- Run dir remains `/scratch/merileo/whale-call-analysis/multispecies_weekend_20260502/prep_runs/dclde_orca_cap200_prep_20260502T202327Z`.

Next heartbeat should monitor job `13161097` and, if it completes, judge the DCLDE prep on the filtered 397-row subset.

## Heartbeat 2026-05-02 18:10 PDT

DCLDE prep validation-filter retry:

- Retry job `13161097` failed on another unreadable UAF file: `dclde_UAF_NGOS_Field_SondTrap__20210615_1705.wav`.
- Rather than continue one file at a time, ran a preflight validation pass over the currently selected staged audio with `soundfile.info`.
- The validation pass found one remaining unreadable audio file and filtered it from the manifests.
- Filtered DCLDE prep subset now has `394` selected rows:
  - `KW`: 194
  - `AB`: 103
  - `HW`: 64
  - `UndBio`: 33
- New active retry job: `13163203`.
- Run dir remains `/scratch/merileo/whale-call-analysis/multispecies_weekend_20260502/prep_runs/dclde_orca_cap200_prep_20260502T202327Z`.
- At last check `13163203` was running on `m1`, with MAT generation restarted on the validated filtered subset.

Next heartbeat should monitor job `13163203`, then inspect coverage/leakage on the 394-row subset if it completes.

## Heartbeat 2026-05-02 18:45 PDT

DCLDE prep completion:

- CPU retry job `13163203` completed successfully in `00:16:10`.
- Filtered DCLDE subset after staged-audio validation: `394` selected rows, with `194` KW positives and `200` hard-negative/confounder rows (`AB`: 103, `HW`: 64, `UndBio`: 33).
- MAT generation wrote `394` report rows and `392` unique MAT files. The difference is explained by two duplicate output windows from duplicate annotation rows:
  - `dclde_ONC_BarkleyCanyon__ICLISTENHF1251_20140101T114159.851Z.wav_277.5s_278.3s_trainstyle.mat`
  - `dclde_OrcaSound_orcasound_lab__1562337136_0005.wav_0.0s_1.1s_trainstyle.mat`
- Grouped splits are leak-free: `leaked_group_count` is `0`.
- Split coverage:
  - Train: `273` rows, `127` groups, `135` Oo/call positives, `138` background-confounders.
  - Val: `59` rows, `27` groups, `30` Oo/call positives, `29` background-confounders.
  - Test: `62` rows, `28` groups, `29` Oo/call positives, `33` background-confounders.

Decision:

- DCLDE cap200 prep passes the bounded Oo-repair gate with a caveat: training should de-duplicate by `mat_path,split` so duplicate annotation windows do not get double weight.
- Added a standardizer de-duplication option and a dedicated bounded E06 submitter for ONC balanced100 + BioDCASE train50 + DCLDE cap200 species+call training.
- Next step is a single bounded GPU experiment, not a broad sweep: `E06_onc_biod_dclde_oo_repair_species_call`, 20 epochs, W&B group `weekend-20260502-dclde`, gated against E01 ONC macro/background FP.

## Heartbeat 2026-05-02 19:30 PDT

E06 launch cleanup and submission:

- Pushed and pulled commit `2366cca`, adding the DCLDE Oo-repair submitter and standardizer de-duplication.
- Initial E06 submission `13166277` was immediately canceled after `00:00:09` because the manifest dry-run exposed `species:OD` in the vocabulary. Root cause: standardized rows with intentionally blank canonical labels fell through to legacy source-field parsing during vocabulary construction.
- Pushed and pulled commit `6cea425`, which makes rows with `canonical_schema_version` respect blank canonical `label_ids` instead of falling back to source fields. This keeps OD demoted to analysis metadata/background.
- Nibi focused checks passed after the fix:
  - `PYTHONPATH=. python -m unittest discover -s tests -p "test_multilabel.py"`
  - `PYTHONPATH=. python -m unittest discover -s tests -p "test_dclde_killer_whale_manifest.py"`
- Clean E06 dry-run produced a 16-label vocabulary with only primary species `Bm`, `Bp`, `Mn`, `Oo`; `species:OD` is no longer trainable.
- Submitted bounded GPU job `13166524`.
- Run dir: `/scratch/merileo/whale-call-analysis/multispecies_weekend_20260502/runs/E06_onc_biod_dclde_oo_repair_species_call_20260503T022802Z`.
- Manifest: `/scratch/merileo/whale-call-analysis/multispecies_weekend_20260502/manifests/E06_onc_biod_dclde_oo_repair_species_call/standardized_manifest.csv`.
- W&B group: `weekend-20260502-dclde`.
- At submission, job `13166524` was pending for GPU node availability; no other weekend training was launched.

Next heartbeat should monitor `13166524`, then compute ONC-only calibrated primary-species macro/micro F1 and background any-primary FP rate from the run outputs when it completes.

## Heartbeat 2026-05-02 19:45 PDT

E06 DCLDE Oo-repair result:

- GPU job `13166524` completed successfully in `00:01:05`.
- Run dir: `/scratch/merileo/whale-call-analysis/multispecies_weekend_20260502/runs/E06_onc_biod_dclde_oo_repair_species_call_20260503T022802Z`.
- ONC-only calibrated primary-species metrics using per-label thresholds:
  - Macro F1: `0.5944`.
  - Micro F1: `0.5660`.
  - Micro precision/recall: `0.4455` / `0.7759`.
  - Background any-primary FP rate: `0.7241` (`21/29` background rows).
- ONC primary per-label metrics:
  - `species:Bm`: F1 `0.9286`, precision `1.0000`, recall `0.8667`, TP/FP/FN `13/0/2`, threshold `0.53`.
  - `species:Bp`: F1 `0.5909`, precision `0.4643`, recall `0.8125`, TP/FP/FN `13/15/3`, threshold `0.51`.
  - `species:Mn`: F1 `0.4898`, precision `0.3529`, recall `0.8000`, TP/FP/FN `12/22/3`, threshold `0.77`.
  - `species:Oo`: F1 `0.3684`, precision `0.2692`, recall `0.5833`, TP/FP/FN `7/19/5`, threshold `0.67`.

Comparison with E01 control:

- E01 ONC macro F1: `0.6371`; background any-primary FP rate: `0.4138` (`12/29`).
- E01 per-label F1: `Bm` `0.8889`, `Bp` `0.5957`, `Mn` `0.5455`, `Oo` `0.5185`.
- E06 improves only `Bm`; `Bp` is effectively flat, while `Mn` and `Oo` are worse and background FP is much worse.

Decision:

- E06 fails the deployability gate: ONC macro is below E01 and background FP rises from `0.4138` to `0.7241`.
- Do not launch the optional no-pos-weight E07: the exception condition was an Oo/Mn improvement with mainly background-FP trouble, but E06 worsens both Oo and Mn F1 versus E01.
- Stop automatic weekend model training. Recommended next work is analysis, not more scaling: inspect E06/E01 Oo and Mn false positives, especially whether DCLDE confounder exposure made ONC backgrounds look too whale-like, and consider a source-aware loss/sampling strategy before any further GPU runs.

## Species-Only Follow-Up Plan 2026-05-03

Rationale:

- The call-type label space made the external-data integration harder before species calibration was stable.
- The next bounded ladder should test whether external data can help the simpler primary-species task without forcing the model to learn call taxonomy and source/domain shift simultaneously.

Planned bounded GPU experiments:

- `E08_onc_dclde_species`: ONC balanced100 + DCLDE cap200, species-only, 20 epochs, final2025 checkpoint init.
- `E09_onc_biod_dclde_species`: ONC balanced100 + BioDCASE train50 + DCLDE cap200, species-only, 20 epochs, final2025 checkpoint init.

Gate:

- Compare against E01 ONC species-only control: ONC macro F1 `0.6372`, background FP `0.4138`.
- Continue only if a species-only external-data run improves or roughly matches ONC macro F1 while keeping background FP near control; otherwise stop and pivot to source-aware sampling/calibration or foundation embeddings.

## Heartbeat 2026-05-03 21:20 PDT

Species-only ladder launch:

- Initial E08/E09 launch attempt hit scratch file-count quota, not space quota: `/scratch` was at `1000K/1000K` files while only using about `707GiB/1024GiB`.
- Removed regenerable DCLDE staged raw audio after MAT generation and cleared scratch-backed caches. Durable DCLDE MATs, manifests, split summaries, metrics, and checkpoints were retained.
- Scratch writes recovered; latest scratch report after cleanup was about `705GiB/1024GiB` and `983K/1000K` files.
- Re-ran submitter dry-runs for both species-only jobs after pulling commit `4ff77a0`.
- Submitted bounded GPU species-only jobs:
  - `13210325` `E08_onc_dclde_species`: ONC balanced100 + DCLDE cap200, species-only, 20 epochs, W&B group `weekend-20260502-species-only`.
    - Run dir: `/scratch/merileo/whale-call-analysis/multispecies_weekend_20260502/runs/E08_onc_dclde_species_20260504T041504Z`.
  - `13210326` `E09_onc_biod_dclde_species`: ONC balanced100 + BioDCASE train50 + DCLDE cap200, species-only, 20 epochs, W&B group `weekend-20260502-species-only`.
    - Run dir: `/scratch/merileo/whale-call-analysis/multispecies_weekend_20260502/runs/E09_onc_biod_dclde_species_20260504T041509Z`.
- Both jobs were pending for GPU priority at submission.

Next heartbeat should monitor jobs `13210325` and `13210326`, compute ONC-only calibrated primary-species metrics when complete, and decide whether to continue with exactly one bounded follow-up or stop and pivot to foundation embeddings/source-aware analysis.

## Maintenance 2026-05-04 00:12 PDT

Scratch cleanup:

- Started from scratch pressure of roughly `705GiB/1024GiB` and `983K/1000K` files.
- Compacted completed weekend `train/example_images/` directories into `example_images.tar.gz` for E01, E02, E03, E04, and E06, preserving metrics, checkpoints, thresholds, predictions, manifests, and summaries.
- Removed stale/dry-run multispecies run directories that had no completed metrics.
- Cleared scratch-backed `.cache` directories:
  - `/scratch/merileo/.cache`: `1` tiny file, `43K`, now empty.
  - `/scratch/merileo/sam3-cache/9379950/apptainer-home/.cache`: already empty.
- Removed regenerable `/scratch/merileo/apptainer-cache/cache/*`, reducing that cache from `17G` / `31` files to empty.
- Removed old bbox dataset cache directories under `/scratch/merileo/whale-call-analysis/finwhale_bbox_runs/dataset_caches`, freeing the largest file-count pressure:
  - `joint_v1_final2025_yolo26n_cap128_monthstrat_20260428T002155Z`: `28,874` files, `14G`.
  - `joint_v1_final2025_yolo26n_cap145_centerfix_orientfix_20260428T181846Z`: `48,226` files, `22G`.
  - `joint_v1_final2025_yolo26n_cap145_centerfix_orientfix_fmax120_20260429T051215Z`: `48,220` files, `21G`.
  - `joint_v1_final2025_yolo26n_cap145_trainmonthfill_20260428T053609Z`: `48,226` files, `22G`.
  - `joint_v1_final2025_yolo26n_full_centerfix_orientfix_sr512_skipctx_tarcache_20260502T0108Z`: `426,605` files, `88G`.
- Kept the recent bbox cache `joint_v1_final2025_yolo26n_full_centerfix_orientfix_sr512_skipctx_freqrobust_20260503T145023Z` because unrelated job `13210969` (`fin_yolo26`) was actively running and this cache may belong to that work.
- Final scratch report after cleanup: about `524GiB/1024GiB` and `480K/1000K` files.

Experiment status discovered during cleanup:

- E08 `13210325` and E09 `13210326` both failed after `00:00:35`.
- Failure was not scratch quota; both logs ended with `RuntimeError: No CUDA GPUs are available` after landing on `g2.nibi.sharcnet`.
- Submitted job scripts did include `#SBATCH --gres=gpu:h100:1` and `--device cuda`, so this looks like a bad GPU allocation/node environment rather than a manifest or storage issue.
- Do not interpret E08/E09 as experimental results. Next bounded action is to diagnose the Nibi GPU allocation issue and resubmit the same species-only ladder only after normal queue/accounting checks.

## Heartbeat 2026-05-04 10:18 PDT

GPU allocation diagnosis and species-only retry:

- Required queue/accounting checks were run before any new Slurm submission.
- Scratch remained healthy on file count: about `480K/1000K` files. Bytes had risen to about `722GiB/1024GiB`, likely due other active/recent work, but file pressure was no longer blocking.
- Scheduler inspection showed `g2` is listed as an H100 node with `Gres=gpu:h100:8`, yet the failed E08/E09 runs on `g2` saw no CUDA device. This looks like a transient bad allocation or node-local CUDA visibility issue.
- Submitted tiny GPU sanity job `13240525` with the same `#SBATCH --gres=gpu:h100:1` request and same Python environment.
- Sanity job completed successfully on `g21` in `00:00:19`:
  - `CUDA_VISIBLE_DEVICES=0`.
  - `torch.cuda.is_available() == True`.
  - `torch.cuda.device_count() == 1`.
  - Device: `NVIDIA H100 80GB HBM3`.
- Because the sanity job passed, resubmitted the bounded species-only ladder:
  - Retry `E08_onc_dclde_species`, job `13240926`.
    - Run dir: `/scratch/merileo/whale-call-analysis/multispecies_weekend_20260502/runs/E08_onc_dclde_species_20260504T173524Z`.
  - Retry `E09_onc_biod_dclde_species`, job `13240928`.
    - Run dir: `/scratch/merileo/whale-call-analysis/multispecies_weekend_20260502/runs/E09_onc_biod_dclde_species_20260504T173543Z`.
- At the first retry monitor check, both jobs were pending for priority with no Slurm logs yet.

Next heartbeat should monitor retry jobs `13240926` and `13240928`; the original failed jobs `13210325` and `13210326` should remain historical failure context only.

## Heartbeat 2026-05-04 11:24 PDT

SSH repair and species-only retry gate:

- Reconnected the ONC VM screen `nibi_multispecies_monitor_fresh4` to Nibi using Pixel 9 Duo approval and attached it to remote tmux session `multispecies_monitor`.
- Retry jobs completed and produced train summaries, threshold sweeps, validation predictions, and example images:
  - E08 retry `13240926`, run dir `/scratch/merileo/whale-call-analysis/multispecies_weekend_20260502/runs/E08_onc_dclde_species_20260504T173524Z`.
  - E09 retry `13240928`, run dir `/scratch/merileo/whale-call-analysis/multispecies_weekend_20260502/runs/E09_onc_biod_dclde_species_20260504T173543Z`.
- ONC-only calibrated primary-species metrics were written on Nibi to `/scratch/merileo/whale-call-analysis/multispecies_weekend_20260502/e08_e09_onc_metrics_20260504T1823Z.json`.

E08 ONC+DCLDE species-only:

- ONC macro F1 `0.5976`; micro F1 `0.5890`.
- Background any-primary FP rate `0.5172` (`15/29`), worse than E01 control `0.4138`.
- Per-label:
  - `species:Bm`: F1 `0.9032`, precision `0.8750`, recall `0.9333`, TP/FP/FN `14/2/1`, threshold `0.50`.
  - `species:Bp`: F1 `0.5789`, precision `0.5000`, recall `0.6875`, TP/FP/FN `11/11/5`, threshold `0.86`.
  - `species:Mn`: F1 `0.5333`, precision `0.4000`, recall `0.8000`, TP/FP/FN `12/18/3`, threshold `0.55`.
  - `species:Oo`: F1 `0.3750`, precision `0.3000`, recall `0.5000`, TP/FP/FN `6/14/6`, threshold `0.46`.

E09 ONC+BioDCASE+DCLDE species-only:

- ONC macro F1 `0.6386`; micro F1 `0.6118`.
- Background any-primary FP rate `0.8276` (`24/29`), much worse than E01 control `0.4138`.
- Per-label:
  - `species:Bm`: F1 `0.9333`, precision `0.9333`, recall `0.9333`, TP/FP/FN `14/1/1`, threshold `0.80`.
  - `species:Bp`: F1 `0.6522`, precision `0.5000`, recall `0.9375`, TP/FP/FN `15/15/1`, threshold `0.49`.
  - `species:Mn`: F1 `0.5600`, precision `0.4000`, recall `0.9333`, TP/FP/FN `14/21/1`, threshold `0.68`.
  - `species:Oo`: F1 `0.4091`, precision `0.2812`, recall `0.7500`, TP/FP/FN `9/23/3`, threshold `0.47`.

Decision:

- E08 fails the gate: macro is below E01 (`0.5976` vs `0.6372`), background FP is worse, and Oo is worse.
- E09 nominally matches/slightly beats E01 macro (`0.6386` vs `0.6372`) and improves Bm/Bp/Mn, but it is not deployable because background FP doubles to `0.8276` and Oo remains worse than E01 (`0.4091` vs `0.5185`).
- Do not launch more ResNet training automatically. The external species-only runs confirm the main failure mode: external data is raising whale sensitivity/recall but badly damaging ONC background rejection, especially with BioDCASE included.
- Next bounded work should pivot to analysis: score histograms by source, ONC background false-positive image grids, per-source calibration/domain-shift tables, and a concrete foundation/embedding branch proposal.

## Analysis Pivot 2026-05-04 16:05 PDT

Generated updated source/domain diagnostics in `experiments/weekend_20260502_analysis/` using E01, E04, E06, E08, and E09 outputs.

Key finding:

- Species-only training did not fix the external-data problem. E08 (`ONC+DCLDE`, species-only) fell to ONC macro F1 `0.5976` with background FP `0.5172`. E09 (`ONC+BioDCASE+DCLDE`, species-only) nominally matched E01 macro F1 at `0.6386`, but was not deployable because ONC background FP rose to `0.8276` (`24/29` rows).
- The ONC background false positives are dominated by `species:Oo`: E01 had `8/29` ONC background rows topped by Oo; E09 had `19/29`. DCLDE is increasing Oo sensitivity, but the learned boundary does not transfer to ONC background.
- BioDCASE still appears useful for Bm/Bp recall and macro F1, but it also shifts calibration toward “whale present” on ONC background. Mixed-source metrics therefore look better than deployment risk.
- DCLDE cap200 gave representative orca positives and confounders, but those confounders are not equivalent to ONC deployment background. External negatives are not enough; we need ONC-like hard negatives and ONC-targeted calibration.

Artifacts:

- Report: `experiments/weekend_20260502_analysis/README.md`
- New figures: `onc_background_false_positive_top_labels.png`, `source_background_score_distributions.png`, updated ONC F1/background/FP plots, and E08/E09 contact sheets.
- New tables: `source_domain_metrics.csv`, `score_quantiles_by_source.csv`, `onc_background_top_primary_label_counts.csv`, plus E08/E09 ONC FP/FN tables.

Decision:

- Stop broad ResNet training and do not reintroduce call types yet.
- Next best technical branch is foundation/embedding analysis: extract embeddings for ONC, BioDCASE train50, and DCLDE cap200; train lightweight species-only probes; inspect whether embeddings cluster more by source than species; then test ONC-calibrated thresholds or source-aware/domain-adapted probes.
- If running one more ResNet experiment, it should be a narrow source-aware calibration test only: species-only, source-balanced batches, ONC-background-heavy validation/calibration, and explicit ONC-like Oo/Mn hard negatives. No cap500/cap1000 scale-up from the current evidence.

## Parsing Audit Fix 2026-05-04 19:45 PDT

Visual recheck of the ONC false-positive contact sheets showed that several rows previously summarized as ONC "background" contain clear low-frequency tonal/down-sweep structure. The original metric was therefore too broad: it counted every row with no primary target label (`Bm/Bp/Mn/Oo`) as background, including demoted `OD` rows and pure-negative candidate clips that have not been visually verified as clean.

Code changes:

- Added explicit evaluation buckets in `src/dataset/multilabel.py`:
  - `primary_species_positive`
  - `demoted_nonprimary_signal`
  - `known_nonprimary_signal`
  - `known_call_signal_no_primary`
  - `candidate_background`
  - `reviewed_background`
  - `empty_unverified`
- Changed ONC pure-negative windows from `review_status=reviewed_background` to `review_status=pure_negative_candidate`.
- Updated weekend analysis so future reruns report true reviewed-background FP separately from no-primary FP, and generate contact sheets for `reviewed_background`, `demoted_nonprimary_signal`, and `candidate_background` false positives.
- Focused local tests passed: `tests.test_multilabel` and `tests.test_multispecies_prep_manifest`.

Interpretation:

- The previous background FP numbers should be read as "no-primary-label FP" until the new bucketed analysis reruns on Nibi.
- Do not launch full-scale training until the new `candidate_background` and `reviewed_background` contact sheets are inspected. If pure-negative candidates contain obvious whale calls, those rows should be quarantined or treated as hard/ambiguous negatives rather than clean silent background.
- Nibi reconnect is currently blocked by Duo/SSH closure after selecting Pixel 9. The heartbeat automation now points at commit `5fc306f` and should pull/rerun tests and bucketed analysis once access works.

## Nibi Access Diagnosis 2026-05-05 12:30 PDT

Diagnosed the apparent Nibi SSH failure from the ONC VM. The ONC VM and Nibi account are reachable; the problem is the fresh MFA path used by the newer monitor screens, not Nibi itself.

Findings:

- ONC VM host `Ubuntu-vm15` has an existing SSH ControlMaster socket at `/home/sbialek/.ssh/cm_nibi_finwhale_bbox_final2025`.
- The ControlMaster is healthy: `ssh -S /home/sbialek/.ssh/cm_nibi_finwhale_bbox_final2025 -O check merileo@nibi.alliancecan.ca` reports `Master running`, and commands through the socket land on `l4.nibi.sharcnet` as `merileo`.
- Fresh monitor screens were using `ssh -tt -F /dev/null ... merileo@nibi.alliancecan.ca` without `-S /home/sbialek/.ssh/cm_nibi_finwhale_bbox_final2025`, so they bypassed the already-authenticated connection and forced a new Duo keyboard-interactive login.
- Fresh keyboard-interactive login reaches the Duo menu, but option `2` (`pixel9`) and the one-time passcode attempt both closed the session immediately without establishing Nibi access. The user also reported no Duo push arriving for those attempts.
- Screen `nibi_multispecies_monitor_fresh6` has now been attached through the working ControlMaster path using `ssh -tt -S /home/sbialek/.ssh/cm_nibi_finwhale_bbox_final2025 ... 'tmux new -A -s multispecies_monitor || bash -l'`.

Decision:

- Prefer the ControlMaster socket for all near-term Nibi monitoring and analysis commands. Do not retry Pixel 9 or stale passcodes automatically.
- If the ControlMaster dies, ask the user for a fresh MFA method; likely next choices are Duo option `1` (`google pixel`) or a new passcode.
- The heartbeat automation was updated with this state and should no longer hammer the broken fresh-Duo path.

## Visual Bucket Audit 2026-05-05 12:55 PDT

Continued the bucketed ONC contact-sheet audit after confirming the Nibi ControlMaster was still healthy.

Visual review covered the ONC `candidate_background` and `demoted_nonprimary_signal` false-positive contact sheets for E01, E04, E06, E08, and E09:

- E01 candidate-background sheet: 8 false-positive clips.
- E04 candidate-background sheet: 10 false-positive clips.
- E06 candidate-background sheet: 11 false-positive clips.
- E08 candidate-background sheet: 9 false-positive clips.
- E09 candidate-background sheet: 14 false-positive clips.
- Demoted-nonprimary sheets for the same runs, totaling the rows where the model fired on OD/non-primary signal rows.

Findings:

- The current ONC validation artifacts contain no true `reviewed_background` rows. The prior "background FP" metric is a mixed no-primary-label metric, not a clean-background deployment metric.
- The `candidate_background` sheets are not visually clean. Many clips show high-energy vertical broadband structure, tonal-looking energy, or other ambiguous signal-like patterns. Some may be artifacts rather than whale calls, but they are not safe to use as reviewed clean background without manual review.
- The `demoted_nonprimary_signal` sheets clearly contain acoustic events and should remain separate from clean background. Counting these as background false positives made the model look worse in a misleading way, while also hiding a real ontology/review issue.
- E09 remains especially risky: after BioDCASE+DCLDE species-only training it fires on `14/15` ONC candidate-background rows and `10/14` demoted-nonprimary rows.

Decision:

- Full-scale species-first training remains blocked.
- Next step should be to create a reviewed ONC background/calibration bucket and quarantine or explicitly mark ambiguous candidate-background clips before using them as deployment gate negatives.
- Treat current candidate-background rows as `needs_review` or `ambiguous_hard_negative`, not as clean background.
- Embedding/foundation-model analysis remains attractive, but it should use the same corrected buckets so it does not inherit the same background-label ambiguity.
